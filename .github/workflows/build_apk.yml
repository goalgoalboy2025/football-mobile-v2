name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    # 1. 环境准备
    - name: Setup Java
      uses: actions/setup-java@v3
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'

    # 2. 智能构建 (自动寻找项目目录)
    - name: Smart Build
      run: |
        echo "🔍 Searching for main.py..."
        # 查找 main.py，排除隐藏目录
        MAIN_FILE=$(find . -type f -name "main.py" -not -path "*/.*" | head -n 1)
        
        if [ -z "$MAIN_FILE" ]; then
          echo "❌ Error: main.py not found in the repository!"
          echo "📂 Current file structure:"
          ls -R
          exit 1
        fi
        
        echo "✅ Found main app file: $MAIN_FILE"
        APP_ROOT=$(dirname "$MAIN_FILE")
        echo "📂 Project Root: $APP_ROOT"
        
        # 进入项目目录
        cd "$APP_ROOT"
        
        echo "📦 Installing dependencies..."
        pip install --upgrade pip
        # 强制安装 flet，确保构建工具存在
        pip install flet
        
        if [ -f "requirements.txt" ]; then
          echo "📄 Found requirements.txt, installing..."
          pip install -r requirements.txt
        else
          echo "⚠️ No requirements.txt found, installing detected packages..."
          # 根据代码分析，您的项目需要这些库
          pip install requests beautifulsoup4 pytz
        fi
        
        echo "🚀 Starting Flet Build..."
        # 使用 -v 开启详细日志
        flet build apk --verbose

    # 3. 上传结果 (匹配所有生成的 APK)
    - name: Upload APK
      if: success()
      uses: actions/upload-artifact@v3
      with:
        name: football-fixtures-apk
        path: |
          **/*.apk
